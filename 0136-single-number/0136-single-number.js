/**
 * @param {number[]} nums
 * @return {number}
 */
var singleNumber = function(nums) {
   nums.forEach(element => {
    if((nums.indexOf(element)==nums.lastIndexOf(element)))
       a = element 
})
   return a
};